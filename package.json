{
  "name": "orc-stomp-timing",
  "version": "1.3.1",
  "description": "Offline UHF RFID Race Timing System",
  "main": "index.html",
  "scripts": {
    "prestart": "kill-port 8080",
    "start": "http-server -p 8080 -c-1 -o /index.html",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fake-indexeddb": "^5.0.1",
    "happy-dom": "^12.10.3",
    "http-server": "^14.1.1",
    "kill-port": "^2.0.1",
    "vitest": "^0.34.6"
  }
}
